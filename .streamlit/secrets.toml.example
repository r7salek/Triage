# Copy these values into Streamlit Community Cloud > Advanced settings > Secrets.
# Do not commit a real .streamlit/secrets.toml file.

# Optional. The app works without OpenAI and falls back to deterministic summaries.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-4.1-mini"
